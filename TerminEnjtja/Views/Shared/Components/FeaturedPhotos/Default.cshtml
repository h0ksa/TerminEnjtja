@model List<PhotoItem>

@foreach (var photo in Model)
{
    <img src="@photo.ImageUrl" alt="@photo.Title"
         class="photo-item"
         loading="lazy"
         onerror="this.onerror=null;this.classList.add('image-error');this.src='/images/placeholder.jpg'">
}